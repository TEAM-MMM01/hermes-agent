# CRM README

Stdlib SQLite CRM. Run `python crm/crm.py --help` and each subcommand with `--help`. Status suggestions: new, contacted, qualified, diagnostic_sold, build_proposed, closed_won, closed_lost.
